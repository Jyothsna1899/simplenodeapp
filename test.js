const assert = require('assert');
describe('Simple Math Test', () => {
 it('should return 5', () => {
        assert.equal(2 + 3, 5);
    });
 it('should return 9', () => {
        assert.equal(3 * 3, 9);
    });
});
